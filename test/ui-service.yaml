apiVersion: v1
kind: Service
metadata:
  labels:
    # app: guestbook
    io.kompose.service: ui
  name: guestbook
spec:
  ports:
  - port: 8084
    protocol: TCP
    targetPort: http-server
  selector:
    # app: guestbook
    io.kompose.service: ui
  type: LoadBalancer